framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - '%kernel.project_dir%/assets/'
        excluded_patterns:
            - '%kernel.project_dir%/assets/react/*'

